---
import Heading from "@/components/Heading";
import CTABlock from "@/components/CTABlock.astro";
import StraightArrow from "@/assets/straight-arrow.png";
import Counter from "@/components/Counter.jsx";

// Props (all optional)
const { id = "about", class: className = "", reversed = false } = Astro.props;

import Video from "/Did-You-Fail-The-CPA-Exam_-Maybe-Its-not-your-Faultseptember.mp4";
import ArrowHeading from "@/components/ArrowHeading.astro";
import FloatingCard from "@/components/FloatingCard.astro";
import { Image } from "astro:assets";
---

<section
  id={id}
  class={`relative space-y-10 min-h-[60vh] lg:min-h-[100vh]
           flex flex-col items-center justify-between 
           py-16 md:py-24 text-light-primary ${className}`}
>
  <!-- Video -->
  <div
    class="relative inset-0 inner-section lg:w-4/6 h-full text-center flex flex-col gap-5 text-light-primary"
  >
    <!-- <Heading tagName="span" className="h3 text-center uppercase">
      Inside The i75 CPA Review Course
    </Heading> -->
    <ArrowHeading
      tagName="h2"
      className="h3 font-bold text-white uppercase tracking-wider bg-primary px-4 py-2 rounded-lg"
      containerClass="flex items-center justify-center gap-4 mb-6"
    >
      Inside The i75 CPA Review Course
    </ArrowHeading>
    <video
      class="w-full h-full object-cover"
      src={Video}
      autoPlay
      controls
      loop
      muted></video>
    <!-- Highway Floating Card -->
    <!-- <FloatingCard
      position="-bottom-6 -left-6"
      exit
      ariaLabel="Rated videos sign"
    >
      <div class="flex flex-col lg:flex-row items-center gap-2 mb-2">
        <Image
          src={StraightArrow}
          alt="Right pointed arrow"
          class="w-7 h-6"
          loading="lazy"
        />
        <p class="text-lg font-bold text-light-primary">
          #<Counter start={0} end={1} duration={300} client:visible />
          Rated Videos
        </p>
      </div>
      <p class="text-xs text-light-primary/90">All Narrated By Darius Clark!</p>
    </FloatingCard>

    <FloatingCard position="top-30 -right-6">
      <div class="text-center">
        <div
          class="text-2xl font-bold text-light-primary flex items-center justify-center gap-1"
        >
          <Image
            src={StraightArrow}
            alt="Right pointed arrow"
            class="w-6 h-5"
            loading="lazy"
          />
          <Counter start={0} end={10} duration={2000} client:visible />k+
        </div>
        <div class="text-xs text-light-primary">Students helped</div>
      </div>
    </FloatingCard> -->
  </div>
  <div
    class="inner-section mx-auto max-w-3xl flex flex-col justify-center items-center gap-6"
  >
    <p class="text-2xl text-center leading-relaxed">
      Welcome to i75 CPA Review, <span class="font-bold"
        >your clear and direct road to passing the CPA exam.</span
      > Led by Darius Clark, our course is designed to <span class="font-bold"
        >simplify complex topics</span
      > and offer focused, practical insights that help you efficiently prepare for
      success. <span class="font-bold"
        >Whether you've struggled with other courses or are just starting your
        CPA journey</span
      >, i75 CPA Review is here to guide you every step of the way.
    </p>
    <CTABlock
    showQuote={false}
    showSecondaryButton={true}
    />
  </div>
</section>
